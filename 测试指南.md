[•] 提供人工测试指南
+ Thought: 98ms
🧪 md-bundle-v2 人工测试指南
1️⃣ 环境准备
# 进入项目目录
cd /Users/mason/ToHighs/md-bundle

# 安装依赖（如果还没安装）
pnpm install

# 启动开发服务器
pnpm --filter @md-bundle/web dev
默认会在 http://localhost:5173 启动。
2️⃣ 基础功能测试
测试 1: 三模式工作区
1. 打开浏览器访问 http://localhost:5173
2. 点击「立即打开文档」或拖拽一个 .md 文件
3. 测试顶部的三个模式按钮：
- ✏️ 编辑模式 - 应看到装饰渲染（标题、加粗、列表、callout）
- 💻 源码模式 - 应看到纯 Markdown 源码
- 👁️ 预览模式 - 应看到渲染后的 HTML（居中 800px）
4. 在源码模式输入 # 新标题，切回编辑模式，按 Cmd/Ctrl+Z 应能撤销
测试 2: 多页签
1. 打开多个文档（拖拽多个 .md 文件）
2. 页签条应显示多个页签
3. 点击页签切换，内容应独立
4. 关闭页签时如有未保存内容，应弹出三按钮确认框：
- 「保存并关闭」
- 「仍要关闭」
- 「取消」
测试 3: 左栏与资源管理
1. 点击工作区左侧的「展开」按钮（幽灵图标）
2. 切换到「资源」页签
3. 拖入图片到编辑器，应自动插入引用并显示在资源列表
4. 未被引用的资源应显示黄色「孤儿」标记
测试 4: 大纲导航
1. 打开包含多级标题的文档
2. 点击工作区右上角的「大纲」按钮（幽灵图标）
3. 应弹出毛玻璃浮层，显示文档大纲
4. 点击大纲项，应滚动到对应标题
5. 按 Esc 或点击钉住按钮可关闭
3️⃣ FSA 功能测试（仅 Chromium）
⚠️ 需要 Chrome/Edge，且网站必须通过 HTTPS 或 localhost 访问
测试 5: 授权文件夹
1. 点击左栏「文件」页签
2. 点击「授权文件夹」按钮
3. 选择本地的一个文件夹
4. 文件树应显示该文件夹下的 .md 和 .mdpkg 文件
测试 6: 文件树操作
1. 打开文件 - 点击树中的 .md 文件，应在编辑器打开
2. 新建文件 - 点击工具栏的 + 按钮，输入文件名，应创建新文件
3. 重命名 - 点击文件名旁的✎按钮，输入新名称
4. 删除 - 点击✕按钮，应弹出确认框（提示"不进回收站"）
5. 刷新 - 点击刷新按钮重新扫描目录
测试 7: 拖入复制
1. 从系统文件管理器拖拽一个文件到文件夹节点
2. 文件应被复制到该文件夹下
4️⃣ 保存模型测试
测试 8: 单一主按钮
1. 有 FSA + 新文档：
- 主按钮显示「保存」
- 点击应弹出 showSaveFilePicker
- 保存后应关联句柄（后续保存直接写回）
2. 有 FSA + 已关联句柄：
- 主按钮显示「保存」
- 点击应直接写回原文件（无弹窗）
3. 无 FSA（用 Firefox 测试）：
- 主按钮显示「下载」
- 点击应下载文件
测试 9: 导出菜单
1. 点击顶栏「导出 ▾」
2. 测试四种导出格式：
- .md - 纯文本
- .mdpkg - 含图片的打包
- HTML - 自包含 HTML
- PNG - 长图
5️⃣ 主题与分享测试
测试 10: 主题三态
1. 点击顶栏主题按钮（◐）
2. 测试三个选项：
- 「跟随系统」
- 「深色」
- 「浅色」
3. 切换后整个界面应即时变色
4. 刷新页面应记住上次选择
测试 11: 邀请链接
1. 点击顶栏「分享 ▾」→「复制邀请链接」
2. 应生成类似 ?ref=invite&by=文字打包师 的 URL
3. 在新标签页打开该 URL
4. 应显示 InviteView 变体页面：
- 大号昵称「@文字打包师」
- 网站卖点
- 「打开 MD-Bundle」按钮
- 网址 bundle.jianxi.me 可见
测试 12: 邀请卡
1. 点击「分享 ▾」→「邀请卡 ▾」
2. 应显示当前卡型名 +「换一款」
3. 选择不同卡型，应渲染不同布局的卡片
4. 四种卡型结构应明显不同（横版/竖版/宣传/名片）
6️⃣ 移动端测试
1. 打开浏览器开发者工具（F12）
2. 切换到设备模拟器（iPhone 12 Pro 或 Pixel 5）
3. 刷新页面
4. 验证：
- 左栏变为底部抽屉（鸡蛋形按钮）
- 大纲按钮在工作区右上角
- 窄屏默认打开预览模式
- 动作区折叠进「更多」菜单
7️⃣ 整窗拖放测试
1. 落地页拖入：在首页拖拽 .md 文件到页面任意位置
- 应直接打开新页签（无遮罩提示）
2. 编辑器拖图：打开文档后，拖拽图片到编辑器
- 应插入行内图片引用
3. 工作区拖文件：拖拽 .mdpkg 到工作区
- 应打开新页签并预览
8️⃣ 会话恢复测试
1. 打开多个文档，做一些编辑
2. 等待几秒（自动保存到 IndexedDB）
3. 关闭浏览器标签页
4. 重新打开 http://localhost:5173
5. 验证：
- 应恢复之前的所有页签
- 当前页签和内容一致
- 编辑位置（滚动位置）恢复
9️⃣ 降级测试
无 FSA 环境（用 Firefox/Safari）：
- 文件树应隐藏或显示「浏览器不支持」
- 主按钮应显示「下载」
- 其他功能正常
坏邀请参数：
1. 访问 ?ref=invite&by=（空昵称）
2. 应回退到普通落地页（无错误）
📋 测试检查清单
□ 三模式切换正常，undo 跨模式保留
□ 多页签打开/切换/关闭正常
□ 左栏展开/收起，孤儿标记显示
□ 大纲浮层 hover/钉住/Esc 关闭
□ FSA 授权/打开/保存/重命名/删除
□ 主题三态切换并持久化
□ 邀请链接生成与打开
□ 四种邀请卡随机切换
□ 移动端布局适配
□ 整窗拖放无遮罩
□ 会话刷新恢复
□ 无 FSA 降级 graceful
如需我帮你执行某个特定测试或修复发现的问题，请告诉我！