# \.MDE 自包含Markdown格式项目评估

> 目标：`.mde` 单文件格式，把 markdown 正文 \+ 图片资源封装进同一个文件，解决原生 md 图片分散、分享就裂图、资源管理麻烦的痛点。
> 重要前置：**`.mde`****后缀已经被微软 Access 旧编译数据库文件占用（Access MDE）**，会产生文件后缀冲突，公开发布不建议直接使用 `.mde` 作为扩展名，建议备选：`.mdpkg` / `.mdc` / `.mdbox`，内部自用可以继续叫 MDE。
> 
> 

## 一、真实市场痛点（需求侧）

### 强真实痛点，大量用户被困扰

1. **文档分发分享场景（最高频）**
把 md 发给同事、客户、导出交付，图片必须附带整个 assets 文件夹；对方解压路径一变图片全部失效；打包 zip 分发体验很差，远不如单文件 PDF/Word 方便。技术文档、故障报告、会议纪要、项目交付文档是重灾区。

2. **本地笔记工具跨设备迁移**
Obsidian、VSCode\+Markdown 写笔记，附件文件夹一大堆碎图片；复制、移动、同步时图片丢失；大量孤立垃圾图片，清理困难。

3. **保密内网场景**
不能使用公网图床，截图不能外传，不能依赖网络；base64 内嵌 md 文件体积膨胀巨大，git 版本管理灾难。

4. **AI 生成文档场景**
大模型输出带截图、示意图的 markdown，输出一堆图片引用，导出分享非常麻烦，AI Agent 产物缺少便携归档格式。

### 当前市面上已有的两类替代方案

#### 方案 A：Base64 内嵌到原生`.md`

工具：md‑image‑embed、Obsidian Image‑Baker 插件等，直接把图片转 base64 写进 md 正文内部。

- 优点：后缀依然`.md`，普通编辑器可以打开；不需要新格式。

- 致命缺点：文件体积暴涨，git diff 彻底失效，版本控制完全废掉；源码可读性极差；大图片场景完全不可用。

#### 方案 B：Zip 容器格式（\\*\.mdz/ \\*\.mdx 容器版）

本质 zip 压缩包，内部：manifest 清单 \+ \[content\.md\]\(content\.md\) \+ assets 图片目录，单文件后缀，解压就是目录结构。

- 优点：图片保持原始二进制，不 base64 膨胀；可以无损导出回原始 md \+ 图片文件夹；压缩减小体积。

- 缺点：**没有主流编辑器原生支持**，必须专用工具打开 / 导出；GitHub/Gitee、VSCode、Typora 均无法直接预览。

> 现状：**痛点广泛存在，但没有形成行业事实标准**，全部都是小众开源项目，没有主流编辑器原生落地，这就是机会窗口。
> 
> 

## 二、市场需求分层：哪些人需要，哪些人不需要

### ✅ 高价值目标用户（刚需）

1. **企业技术人员、项目经理（你的场景）**
写项目方案、故障报告、评审文档、交付材料，内网环境，经常对外交付 markdown 材料；需要单文件交付，又想保留 markdown 可编辑特性，不想直接输出 PDF。

2. **本地笔记重度用户（Obsidian 用户群体）**
需要把单篇笔记完整导出分享，不希望附带一堆附件文件夹。

3. **AI 工具开发者、Agent 应用**
AI 批量产出带图 markdown 文档，需要归档、导出、交换，不希望一堆散落文件。

4. **内网文档系统、离线知识库**
不能依赖图床，追求离线完整自包含文档。

### ❌ 非目标用户（几乎无需求）

1. **Git 仓库开源文档**：完全不需要。Git 偏好纯文本 md，图片单独存仓库，容器格式不能直接 git diff，仓库提交会很麻烦。

2. **公开博客、网页发布**：优先图床外链，不需要打包图片到本地文件。

3. **轻量简短笔记（无图或少图）**：原生 md 足够。

> 市场规模：**不是大众海量 C 端市场，属于垂直开发者、技术文档、企业内部工具赛道，属于小而美工具，很难做成千万级用户产品，但垂直领域价值明确。**
> 
> 

## 三、是否值得实现：必要性评估

### 🔹值得做的理由（收益）

1. **痛点真实且普遍**，目前没有主流成熟标准化方案，现有开源 mdz/mdx 容器项目活跃度有限，没有形成生态。

2. **填补中间空白**：介于「原生 md（碎文件）」和「PDF（不可编辑）」中间：**可编辑 markdown \+ 单文件携带图片**，交付时既可以给对方\.mde 容器文件，对方也可以一键导出回原始 md \+ 图片文件夹继续修改。

3. **私有化 / 内网场景价值很高**，非常适合信创、企业内部工具链嵌入，可作为自研工具链的配套格式。

4. 技术实现难度中等：zip 容器方案实现成本可控；核心逻辑：读写 zip 包 \+ manifest 清单管理图片映射 \+ md 双向转换。

### 🔹风险与劣势（必须充分权衡）

1. **生态兼容是最大瓶颈**

> 自定义后缀格式最大痛点：VS Code、Typora、GitHub 全部不会原生识别。拿到`.mde`文件的人，如果没有你的工具，**完全无法直接查看内容**。对比：base64\-md 至少任意编辑器能打开看到文字（图片裂）；PDF 到处都能打开。
> 这是最大的短板：**分享给外部同事，对方必须安装你的工具才能打开**，会极大阻碍传播。
> 
> 

2. **版本控制系统不友好**
容器是二进制 zip 包，git 无法 diff 对比文档修改；不适合放进 git 仓库，只能用于**分发、导出、归档**，不适合作为日常源文件格式。

> 定位必须清晰：**MDE 是导出 / 交换 / 归档格式，不替代日常编辑的原生\.md 源文件。**
> 
> 

3. 已有撞名：`.mde` 历史属于 Access 数据库，Windows 系统会产生认知混淆，公开发布强烈建议更换后缀名。

4. 要做完整体验工作量不小：
不仅仅是编解码库，还需要配套：CLI 工具、VSCode 插件、预览器、双向导出（mde ↔ md\+assets 文件夹），否则只是一个玩具格式。

## 四、市场价值分析

### 商业价值

1. **直接变现难度一般**：格式本身很难卖钱；但是可以作为**工具产品的核心差异化卖点**。

> 举例：如果你做文档工具、知识库、AI Agent 工具，内置这个自包含单文件导出能力，会成为竞品没有的亮点；对内网企业项目非常加分。
> 
> 

2. **开源价值高**：如果开源规范 \+ SDK，可以吸引开发者社区；但很难形成像 markdown 那样的广泛生态，最多垂直小众标准。

3. **内部业务价值＞公开 C 端商业价值**
如果你是服务企业项目、信创、内部工具链：**内部落地价值很高，可以解决团队实实在在的文档分发痛苦**。
如果希望做成面向全网大众产品：天花板低，教育用户成本很高，很难普及。

### 和现有方案对比

|方案|单文件|可编辑|Git 友好|任意编辑器打开|大图性能|
|---|---|---|---|---|---|
|原生 md \+ 图片文件夹|❌|✅|✅|✅|很好|
|Base64 内嵌 md|✅|✅|❌|✅|很差，文件膨胀|
|PDF|✅|❌|❌|✅|很好|
|MDE/MDPKG（zip 容器）|✅|✅|❌|❌（需要专用工具）|很好|

## 五、落地建议方案

### 方案 1：内部项目优先落地（推荐）

不对外强行推广格式标准，定位：**导出交换格式，不作为日常源文件**。

1. 修改后缀，避开`.mde`，使用`.mdpkg`；底层基于 zip 容器：

```Plain Text
xxx.mdpkg
├─ manifest.json   #清单：图片映射、元信息、版本
├─ content.md      #原始markdown正文
└─ assets/         #原始图片二进制，不base64
```

2. 提供双向能力：

- `md+assets文件夹 → mdpkg`（打包分发）

- `mdpkg → md+assets文件夹`（解包回到原始工程，继续编辑）

3. 产出：SDK 库 \+ CLI 工具，可选 VSCode 插件预览。

> 团队内部使用收益巨大；对外交付可以给对方两种选择：`.mdpkg`完整包，或者导出 zip 文件夹。
> 
> 

### 方案 2：不做新格式，复用现有工具链（低成本备选）

如果不想维护自定义格式：直接封装现有 MDZ 开源规范，基于现有开源库做二次开发，不用从零设计格式规范。

### ❌不建议：做成 base64 塞进同一个纯文本后缀\.mde

会继承 base64 全部缺陷：体积爆炸、git 不可用，不如 zip 容器方案。

## 六、总结结论

1. **痛点真实存在，但是市场属于垂直小众，不是全民通用大市场。**

2. **有实现价值，但要找准定位：它是【分发 / 归档 / 交换格式】，绝对不要替代日常开发编辑用的原生 md 源文件。**

3. 最大风险不是技术，而是**生态兼容**：没有配套工具的人打不开你的文件，对外传播会受限。

4. 如果你主要服务企业内部、项目交付、自研工具链：**非常值得做，解决实实在在的业务痛点**。
如果你目标是做一款面向普通大众的通用文档格式：不建议，推广成本极高，很难普及。

5. 不建议使用`.mde`作为对外扩展名，历史被 Access 数据库占用，建议改用`.mdpkg`。

如果你需要，我可以进一步输出一份极简的格式规范草案（manifest 定义、目录结构、编解码要点）。

> （注：部分内容可能由 AI 生成）
